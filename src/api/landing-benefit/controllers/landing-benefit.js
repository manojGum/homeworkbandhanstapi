'use strict';

/**
 * landing-benefit controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::landing-benefit.landing-benefit');
